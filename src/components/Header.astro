---
// Import new logo variants
import logo from '../assets/logos/cairf_logo.png'

// Import icons for mobile navigation
import hamburgerIcon from '../assets/icons/hamburger.svg?raw'
import crossIcon from '../assets/icons/cross.svg?raw'

// Import ApplyButton component
import ApplyButton from './ApplyButton.astro'

import Image from 'astro/components/Image.astro';
---

<header class="bg-white shadow-sm border-b border-gray-100 sticky top-0 z-50 py-1 md:py-2">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="flex justify-between items-center h-16">
      <!-- Logo/Title -->
      <div class="flex-shrink-0">
        <!-- Full logo with text -->
        <div>
          <a href="#hero">
            <Image src={logo} alt="Cooperative AI Research Fellowship Logo" layout='constrained' class="h-10 md:h-12 w-auto" />
          </a>
        </div>
      </div>

      <!-- Desktop Navigation -->
      <nav class="hidden md:flex space-x-8 font-sans">
        <a href="#about" class="text-text-base hover:text-gray-600 transition-colors duration-200 font-medium">
          About
        </a>
        <a href="#tracks" class="text-text-base hover:text-gray-600 transition-colors duration-200 font-medium">
          Tracks
        </a>
        <a href="#mentorship" class="text-text-base hover:text-gray-600 transition-colors duration-200 font-medium">
          Mentors
        </a>
        <a href="#partners" class="text-text-base hover:text-gray-600 transition-colors duration-200 font-medium">
          Partners
        </a>
        <a href="#faq" class="text-text-base hover:text-gray-600 transition-colors duration-200 font-medium">
          FAQ
        </a>
      </nav>

      <!-- Desktop Apply Button -->
      <div class="hidden md:block">
        <ApplyButton variant="secondary" />
      </div>

      <!-- Mobile menu button -->
      <div class="md:hidden">
        <button
          id="mobile-menu-button"
          class="text-text-base hover:text-gray-600 focus:outline-none focus:text-gray-600 p-2 rounded-md hover:bg-gray-100 transition-colors duration-200"
          aria-label="Toggle mobile menu"
          type="button"
        >
          <div id="hamburger-icon" class="h-6 w-6" set:html={hamburgerIcon}></div>
          <div id="cross-icon" class="h-6 w-6 hidden" set:html={crossIcon}></div>
        </button>
      </div>
    </div>

    <!-- Mobile Navigation Menu -->
    <div id="mobile-menu" class="hidden md:hidden">
      <div class="px-2 pt-2 pb-3 space-y-1 bg-white font-sans">
        <a href="#about" class="block px-3 py-2 text-text-base hover:text-gray-600 transition-colors duration-200 font-medium">
          About
        </a>
        <a href="#key-details" class="block px-3 py-2 text-text-base hover:text-gray-600 transition-colors duration-200 font-medium">
          Key Details
        </a>
        <a href="#tracks" class="block px-3 py-2 text-text-base hover:text-gray-600 transition-colors duration-200 font-medium">
          Research Tracks
        </a>
        <a href="#mentorship" class="block px-3 py-2 text-text-base hover:text-gray-600 transition-colors duration-200 font-medium">
          Mentorship
        </a>
        <a href="#application-process" class="block px-3 py-2 text-text-base hover:text-gray-600 transition-colors duration-200 font-medium">
          Application Process
        </a>
        <a href="#selection-criteria" class="block px-3 py-2 text-text-base hover:text-gray-600 transition-colors duration-200 font-medium">
          Selection Criteria
        </a>
        <a href="#why-cooperative-ai" class="block px-3 py-2 text-text-base hover:text-gray-600 transition-colors duration-200 font-medium">
          Why Cooperative AI
        </a>
        <a href="#why-south-africa" class="block px-3 py-2 text-text-base hover:text-gray-600 transition-colors duration-200 font-medium">
          Why South Africa
        </a>
        <a href="#timeline" class="block px-3 py-2 text-text-base hover:text-gray-600 transition-colors duration-200 font-medium">
          Timeline
        </a>
        <a href="#partners" class="block px-3 py-2 text-text-base hover:text-gray-600 transition-colors duration-200 font-medium">
          Partners
        </a>
        <a href="#faq" class="block px-3 py-2 text-text-base hover:text-gray-600 transition-colors duration-200 font-medium">
          FAQ
        </a>
        <a href="#webinar" class="block px-3 py-2 text-text-base hover:text-gray-600 transition-colors duration-200 font-medium">
          Webinar Recording
        </a>
        <div class="p-6 flex justify-center">
          <ApplyButton variant="secondary" />
        </div>
      </div>
    </div>
  </div>
</header>

<script>
  // Mobile menu toggle with proper icon switching
  document.addEventListener('DOMContentLoaded', function() {
    const button = document.getElementById('mobile-menu-button');
    const menu = document.getElementById('mobile-menu');
    const hamburgerIcon = document.getElementById('hamburger-icon');
    const crossIcon = document.getElementById('cross-icon');
    
    if (button && menu && hamburgerIcon && crossIcon) {
      button.onclick = function() {
        const isHidden = menu.classList.contains('hidden');
        if (isHidden) {
          menu.classList.remove('hidden');
          hamburgerIcon.classList.add('hidden');
          crossIcon.classList.remove('hidden');
        } else {
          menu.classList.add('hidden');
          hamburgerIcon.classList.remove('hidden');
          crossIcon.classList.add('hidden');
        }
      };
      
      // Close menu when clicking on links
      const links = menu.querySelectorAll('a');
      links.forEach(link => {
        link.onclick = function(e) {
          const anchorElement = (e.target as HTMLElement).closest('a') as HTMLAnchorElement;
          if (!anchorElement) return;
          
          const href = anchorElement.getAttribute('href');
          
          // Check if this is an external link (ApplyButton) or internal anchor link
          if (href && (href.startsWith('http') || href.startsWith('https') || href.startsWith('mailto:'))) {
            // External link - allow normal navigation, just close the menu
            menu.classList.add('hidden');
            hamburgerIcon.classList.remove('hidden');
            crossIcon.classList.add('hidden');
            return; // Don't prevent default, let the link work normally
          }
          
          // Internal anchor link - handle smooth scrolling
          e.preventDefault();
          // Close menu immediately
          menu.classList.add('hidden');
          hamburgerIcon.classList.remove('hidden');
          crossIcon.classList.add('hidden');
          
          // Small delay to ensure menu is closed before scroll
          setTimeout(() => {
            if (href) {
              const targetElement = document.querySelector(href);
              if (targetElement) {
                // Get header height for offset
                const header = document.querySelector('header');
                const headerHeight = header ? header.offsetHeight : 64;
                
                // Calculate target position with header offset
                const targetPosition = (targetElement as HTMLElement).offsetTop - headerHeight;
                
                window.scrollTo({
                  top: targetPosition,
                  behavior: 'smooth'
                });
              }
            }
          }, 100);
        };
      });
    }
  });
</script>
